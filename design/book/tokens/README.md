# Canonical Book Design Tokens

Approved typography, spacing, color, page-geometry, code, table, diagram, callout, and icon/emoji tokens belong here.

All tokens must be language-independent. Language may select content and locale data only; publication format may select format-specific output behavior only.
