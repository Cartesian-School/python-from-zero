# Figma Handoff

Store Figma design-file links, page/node IDs, component mappings, review notes, and approved handoff metadata here.

Figma remains a design and review surface only. The canonical book pipeline remains the sole publishing implementation.

## v1 build — IN PROGRESS, BLOCKED

- **Figma file:** <https://www.figma.com/design/m19Q51E0vmVek8r4TCJCd8> ("Cartesian School — Book Design System v1")
- **File key:** `m19Q51E0vmVek8r4TCJCd8`
- **Status:** Variable/token foundation complete (6 collections, 87 variables). Text
  styles and all 6 canonical reference frames are **not yet created** — blocked by the
  Figma MCP Starter-plan rate limit (~20 calls/month), confirmed exhausted on two
  separate attempts this session.
- **Agreed v1 scope (reduced):** finish text styles + the Typography reference frame
  only; the other 5 archetype frames are deferred to a follow-up pass. Resume once the
  Figma plan is upgraded (expected in ~3 days) or the monthly quota resets.
- Full build log, node/variable IDs, font decisions, and resume steps:
  see [`HANDOFF-v1-build-log.md`](HANDOFF-v1-build-log.md).
