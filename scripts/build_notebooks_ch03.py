#!/usr/bin/env python3
"""Строит 4 ноутбука практики для Главы 3."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from notebook_lib import NotebookBuilder

ROOT = Path(__file__).resolve().parent.parent
OUT_DIR = ROOT / "notebooks" / "chapter-03"


def build_01_first_program() -> None:
    nb = NotebookBuilder()
    nb.md("""\
# 03-01 · Создание и запуск программ

Практика к разделу [«Создание и запуск программ на Python»](../../site/chapters/glava-03/03-01-sozdanie-i-zapusk-programm.html).""")
    nb.md("## Цель\n\nПочувствовать, как код превращается в результат — здесь роль «файла и запуска» "
          "играет ячейка кода.")
    nb.md("## Что нужно знать\n\n`print()` выводит текст на экран — знакомая команда из главы 1.")
    nb.md("## Краткое напоминание\n\nВ настоящем `.py`-файле код запускают командой "
          "`python имя_файла.py`. Здесь для практики каждая ячейка — это как маленькая отдельная "
          "программа.")
    nb.md("## Рабочий пример")
    nb.code('''print("Привет, Python!")''')
    nb.md("## Эксперимент 1\n\nИзмените текст приветствия на своё.")
    nb.code('''print("Привет, Cartesian School!")''')
    nb.md("## Эксперимент 2\n\nДобавьте вторую строку `print()` с вашим именем.")
    nb.code('''print("Привет, Python!")
print("Меня зовут Cartesian")''')
    nb.md("## Типичная ошибка\n\nВ очень старых учебниках по Python 2 можно встретить "
          "`print \"Привет\"` без скобок — в Python 3 (и тем более в 3.14) это ошибка синтаксиса.")
    nb.code('''print "Привет"''', raises=True)
    nb.md("## Исправление\n\nВ Python 3 `print` — обычная функция, аргумент передают в скобках.")
    nb.code('''print("Привет")''')
    nb.md("## Задание ★ Базовая практика\n\nВыведите три строки: своё имя, свой город и любимое "
          "число.")
    nb.code('''print("Cartesian")
print("Москва")
print(42)''')
    nb.md("## Самостоятельная практика ★★\n\nВыведите небольшое двустишие или цитату — минимум "
          "две строки — используя два отдельных вызова `print()`.")
    nb.code('''print("Код за кодом, шаг за шагом —")
print("так рождается программа.")''')
    nb.write(OUT_DIR / "03-01-first-program.ipynb")
    print(f"Записано: 03-01-first-program.ipynb ({len(nb)} ячеек)")


def build_02_python_shell() -> None:
    nb = NotebookBuilder()
    nb.md("""\
# 03-02 · Интерактивный режим (Python Shell)

Практика к разделу [«Интерактивный режим Python (Python Shell)»](../../site/chapters/glava-03/03-02-interaktivny-rezhim.html).""")
    nb.md("## Цель\n\nПонять, чем интерактивная оболочка отличается от обычного файла — и "
          "заметить, что ноутбук Jupyter устроен очень похоже.")
    nb.md("## Что нужно знать\n\nВ Python Shell (`>>>`) каждая введённая строка сразу показывает "
          "результат.")
    nb.md("## Краткое напоминание\n\nВ настоящей оболочке `>>> 2 + 2` тут же печатает `4` — без "
          "`print()`. Проверим, работает ли то же самое в ячейке Jupyter.")
    nb.md("## Рабочий пример")
    nb.code('''2 + 2''')
    nb.md("""\
## Эксперимент 1

Сработало! Ячейка Jupyter, как и Python Shell, автоматически показывает результат **последнего**
выражения в ячейке — и то, и другое построено на одной и той же идее REPL. Но это работает только
для *последней* строки — проверим.""")
    nb.code('''1 + 1
2 + 2
3 + 3''')
    nb.md("## Эксперимент 2\n\nА если результат нужен на каждой строке — используем `print()`, "
          "как в обычном файле.")
    nb.code('''print(1 + 1)
print(2 + 2)
print(3 + 3)''')
    nb.md("""\
## Типичная ошибка

В настоящей оболочке `>>> имя = "Cartesian"` **не** печатает результат — присваивание само по
себе не является выражением со значением для показа. Начинающие иногда ждут вывод и после
присваивания и удивляются, что его нет.""")
    nb.code('''name = "Cartesian"  # ничего не выводится — это нормально''')
    nb.md("## Исправление\n\nЧтобы увидеть значение переменной, нужно отдельно её вывести.")
    nb.code('''name = "Cartesian"
print(name)''')
    nb.md("## Задание ★ Базовая практика\n\nВ одной ячейке напишите три арифметических выражения "
          "подряд и оберните каждое в `print()`, чтобы увидеть все три результата.")
    nb.code('''print(5 + 5)
print(9 - 4)
print(6 * 6)''')
    nb.write(OUT_DIR / "03-02-python-shell.ipynb")
    print(f"Записано: 03-02-python-shell.ipynb ({len(nb)} ячеек)")


def build_03_math() -> None:
    nb = NotebookBuilder()
    nb.md("""\
# 03-03 · Ваша оболочка умеет считать

Практика к разделу [«Ваша оболочка умеет считать»](../../site/chapters/glava-03/03-02-interaktivny-rezhim.html#schitaet).""")
    nb.md("## Цель\n\nИспользовать Python как калькулятор — сложение, вычитание, умножение, "
          "деление.")
    nb.md("## Что нужно знать\n\nОсновные знаки: `+`, `-`, `*` (умножение), `/` (деление).")
    nb.md("## Краткое напоминание\n\n`/` всегда возвращает дробное число, даже если результат "
          "делится нацело — подробнее об этом в главе 4.")
    nb.md("## Рабочий пример")
    nb.code('''print(2 + 2)
print(10 - 4)
print(3 * 7)
print(10 / 4)''')
    nb.md("## Эксперимент 1\n\nПопробуйте разделить два числа так, чтобы результат делился "
          "нацело — например, 10 на 2. Каким будет результат: `5` или `5.0`?")
    nb.code('''print(10 / 2)''')
    nb.md("## Эксперимент 2\n\nПорядок операций в Python такой же, как в математике: сначала "
          "умножение и деление, потом сложение и вычитание. Проверьте.")
    nb.code('''print(2 + 3 * 4)   # сначала 3 * 4, потом + 2
print((2 + 3) * 4) # скобки меняют порядок''')
    nb.md("## Задание ★ Базовая практика\n\nПосчитайте, сколько минут в трёх с половиной часах, "
          "используя выражение с `*`.")
    nb.code('''print(3.5 * 60)''')
    nb.md("## Проверка результата")
    nb.code('''assert 3.5 * 60 == 210.0
print("Проверка пройдена: 210 минут.")''')
    nb.md("## Дополнительная задача ★★★\n\nПосчитайте среднее трёх чисел (например, ваших "
          "оценок или любых трёх чисел) одним выражением, используя скобки.")
    nb.code('''a, b, c = 8, 9, 10
print((a + b + c) / 3)''')
    nb.write(OUT_DIR / "03-03-math.ipynb")
    print(f"Записано: 03-03-math.ipynb ({len(nb)} ячеек)")


def build_04_print() -> None:
    nb = NotebookBuilder()
    nb.md("""\
# 03-04 · Вывод данных с помощью Python

Практика к разделу [«Вывод данных с помощью Python»](../../site/chapters/glava-03/03-03-vyvod-dannyh.html).""")
    nb.md("## Цель\n\nОсвоить `print()` подробно: несколько значений, `sep`, `end`.")
    nb.md("## Что нужно знать\n\n`print()` умеет выводить несколько значений через запятую.")
    nb.md("## Краткое напоминание\n\n- по умолчанию значения разделяются пробелом;\n"
          "- `sep=\"...\"` меняет разделитель;\n"
          "- `end=\"...\"` меняет символ в конце (по умолчанию — перевод строки).")
    nb.md("## Рабочий пример")
    nb.code('''print("Возраст:", 10, "лет")''')
    nb.md("## Эксперимент 1\n\nИзмените разделитель на дефис.")
    nb.code('''print("2024", "01", "15", sep="-")''')
    nb.md("## Эксперимент 2\n\nИспользуйте `end=\"\"`, чтобы два вызова `print()` вывелись в одну "
          "строку без переноса.")
    nb.code('''print("Загрузка", end="")
print("...", end="")
print("готово!")''')
    nb.md("""\
## Задание ★ Базовая практика (и практика главы: «выведите своё имя»)

Одной командой `print()` с `sep=" · "` выведите своё имя, город и любимое число.""")
    nb.code('''print("Cartesian", "Москва", 42, sep=" · ")''')
    nb.md("## Типичная ошибка\n\n`sep` и `end` — именованные параметры: их нужно передавать по "
          "имени. Если перепутать порядок и передать их как обычные значения, они попадут в сам "
          "вывод, а не станут разделителем.")
    nb.code('''print("a", "b", "-")  # это НЕ то же самое, что sep="-"''')
    nb.md("## Исправление")
    nb.code('''print("a", "b", sep="-")''')
    nb.md("## Самостоятельная практика ★★\n\nВыведите таблицу из трёх строк вида "
          "`имя — оценка`, используя `sep=\" — \"` в каждом вызове.")
    nb.code('''print("Математика", 5, sep=" — ")
print("Python", 5, sep=" — ")
print("Физика", 4, sep=" — ")''')
    nb.write(OUT_DIR / "03-04-print.ipynb")
    print(f"Записано: 03-04-print.ipynb ({len(nb)} ячеек)")


def build_05_repl_predict() -> None:
    nb = NotebookBuilder()
    nb.md("""\
# 03-05 · Предскажите и проверьте: REPL

Практика к разделу [«REPL как инструмент для исследования»](../../site/chapters/glava-03/03-09-repl-kak-instrument.html).""")
    nb.md("## Цель\n\nПотренироваться предсказывать, что покажет REPL/ячейка, прежде чем "
          "запускать код — и проверить себя с помощью `type()`.")
    nb.md("## Что нужно знать\n\n`type(значение)` показывает тип значения: "
          "`<class 'int'>`, `<class 'str'>` и так далее.")
    nb.md("## Краткое напоминание\n\nЯчейка (как и REPL) сама печатает результат "
          "*последнего* выражения — но не результат `print()`-вызовов, которые печатают "
          "сами по себе, независимо от того, последние они или нет.")
    nb.md("## Рабочий пример")
    nb.code('''type(42)''')
    nb.md("## Эксперимент 1\n\nПрежде чем запускать — предскажите: какой тип у `\"42\"` "
          "(в кавычках)? Затем проверьте.")
    nb.code('''type("42")''')
    nb.md("## Эксперимент 2\n\nПредскажите тип результата `10 / 2` — это будет `int` или "
          "`float`? Затем проверьте.")
    nb.code('''type(10 / 2)''')
    nb.md("## Типичная ошибка\n\nНачинающие иногда ждут, что `dir(42)` покажет что-то "
          "понятное с первого взгляда — на деле это длинный список служебных имён. Это "
          "нормально: пока нужно уметь ЗАПУСКАТЬ `dir()`, а не понимать каждое имя в "
          "списке.")
    nb.code('''len(dir(42)) > 0  # просто убеждаемся, что список не пустой''')
    nb.md("## Задание ★ Базовая практика\n\nВыведите тип каждого из трёх значений: "
          "`3.14`, `True`, `[1, 2, 3]` — тремя отдельными вызовами `print(type(...))`.")
    nb.code('''print(type(3.14))
print(type(True))
print(type([1, 2, 3]))''')
    nb.md("## Самостоятельная практика ★★\n\nИспользуйте `help(print)`, чтобы прочитать "
          "встроенную справку по `print`, а затем выведите текстом одну вещь, которую вы "
          "там узнали (например, имена параметров).")
    nb.code('''help(print)
print("Узнал(а): print принимает sep и end")''')
    nb.write(OUT_DIR / "03-05-repl-predict.ipynb")
    print(f"Записано: 03-05-repl-predict.ipynb ({len(nb)} ячеек)")


def build_06_input() -> None:
    nb = NotebookBuilder()
    nb.md("""\
# 03-06 · input() и первый диалог

Практика к разделу [«input(): программа начинает разговаривать»](../../site/chapters/glava-03/03-10-input-i-dialog.html).""")
    nb.md("## Цель\n\nОсвоить `input()` — первую команду, которая делает программу "
          "по-настоящему интерактивной.")
    nb.md("## Что нужно знать\n\n`input(\"вопрос\")` печатает вопрос, ждёт, пока "
          "пользователь наберёт текст и нажмёт Enter, и возвращает введённый текст.")
    nb.md("""\
## Краткое напоминание

В браузерной практике `input()` тоже работает — Pyodide-раннер покажет настоящее поле
ввода. В рабочем примере ниже мы для надёжности сразу передаём готовый ответ, чтобы
пример проверялся автоматически — но в следующем эксперименте вы наберёте текст сами.""")
    nb.md("## Рабочий пример")
    nb.code('''name = "Cartesian"  # представим, что это ответ, который ввёл пользователь
print("Привет,", name)''')
    nb.md("## Эксперимент 1\n\nТеперь по-настоящему: запустите эту ячейку и введите своё "
          "имя в появившемся поле.")
    nb.code('''name = input("Как вас зовут? ")
print("Привет,", name)''')
    nb.md("## Эксперимент 2\n\n`input()` всегда возвращает **текст** — даже если "
          "пользователь ввёл цифры. Проверим: что покажет `type()`?")
    nb.code('''age_text = input("Сколько вам лет? ")
print(type(age_text))''')
    nb.md("## Типичная ошибка\n\nПопытка сразу использовать введённый текст как число "
          "(например, сложить с числом) приведёт к ошибке — Python не складывает текст и "
          "число напрямую. Полное решение — в следующих главах; здесь достаточно знать, "
          "что `input()` возвращает именно текст.")
    nb.code('''age_text = input("Сколько вам лет? ")
print(age_text + 1)''', raises=True)
    nb.md("## Исправление (предварительно)\n\nПреобразовать текст в число поможет "
          "`int(...)` — подробно разберём это в следующей главе; здесь — только "
          "демонстрация.")
    nb.code('''age_text = input("Сколько вам лет? ")
print(int(age_text) + 1)''')
    nb.md("## Задание ★ Базовая практика\n\nСпросите имя пользователя через `input()` и "
          "выведите приветствие в формате `Привет, <имя>! Рады знакомству.`.")
    nb.code('''name = input("Как вас зовут? ")
print("Привет,", name + "!", "Рады знакомству.")''')
    nb.write(OUT_DIR / "03-06-input.ipynb")
    print(f"Записано: 03-06-input.ipynb ({len(nb)} ячеек)")


def build_07_names() -> None:
    nb = NotebookBuilder()
    nb.md("""\
# 03-07 · Имена и значения

Практика к разделу [«Первые имена и значения»](../../site/chapters/glava-03/03-11-imena-i-znacheniya.html).""")
    nb.md("## Цель\n\nПотренироваться создавать имена (переменные) и обращаться к ним "
          "снова — без ошибки `NameError`.")
    nb.md("## Что нужно знать\n\n`имя = значение` создаёт имя, которое указывает на "
          "значение. Дальше это имя можно использовать вместо самого значения.")
    nb.md("## Краткое напоминание\n\nПравильнее думать не «имя — это коробка со "
          "значением», а «имя указывает на значение» — как стрелка. Одно и то же значение "
          "может быть у нескольких имён сразу.")
    nb.md("## Рабочий пример")
    nb.code('''city = "Warsaw"
print(city)''')
    nb.md("## Эксперимент 1\n\nСоздайте второе имя, указывающее на то же значение, и "
          "убедитесь, что оба имени работают.")
    nb.code('''city = "Warsaw"
town = city
print(city)
print(town)''')
    nb.md("## Эксперимент 2\n\nПерезапишите значение одного из имён и убедитесь, что "
          "второе имя не изменилось — присваивание меняет, на что указывает ИМЕННО ЭТО "
          "имя, а не всё, что на него ссылалось.")
    nb.code('''city = "Warsaw"
town = city
city = "Krakow"
print(city)
print(town)''')
    nb.md("## Типичная ошибка\n\nОбращение к имени, которое ещё не создано (опечатка или "
          "неправильный порядок строк), даёт `NameError`.")
    nb.code('''print(favourite_city)
favourite_city = "Warsaw"''', raises=True)
    nb.md("## Исправление\n\nИмя должно быть создано присваиванием ДО того, как к нему "
          "обращаются.")
    nb.code('''favourite_city = "Warsaw"
print(favourite_city)''')
    nb.md("## Задание ★ Базовая практика\n\nСоздайте три имени — `name`, `city`, "
          "`hobby` — и выведите их одной командой `print()` через запятую.")
    nb.code('''name = "Cartesian"
city = "Warsaw"
hobby = "Python"
print(name, city, hobby)''')
    nb.write(OUT_DIR / "03-07-imena.ipynb")
    print(f"Записано: 03-07-imena.ipynb ({len(nb)} ячеек)")


def build_08_traceback_nameerror() -> None:
    nb = NotebookBuilder()
    nb.md("""\
# 03-08 · Прочитайте traceback: NameError

Практика к разделу [«Ошибки, traceback и как их читать»](../../site/chapters/glava-03/03-13-oshibki-i-traceback.html).""")
    nb.md("## Цель\n\nНаучиться спокойно читать `NameError` и находить причину по "
          "traceback, а не пугаться красного текста.")
    nb.md("## Что нужно знать\n\n`NameError` означает: Python встретил имя, которое ещё "
          "нигде не было создано присваиванием.")
    nb.md("## Краткое напоминание\n\nПоследняя строка traceback — самая важная: в ней "
          "указан тип ошибки и краткое объяснение.")
    nb.md("## Разминка\n\nПрежде чем смотреть на ошибку, запустите одну рабочую ячейку — "
          "чтобы Python-среда точно была готова.")
    nb.code('''print("Готовы искать ошибки!")''')
    nb.md("## Рабочий пример\n\nЗапустите — и внимательно прочитайте последнюю строку "
          "вывода.")
    nb.code('''print(usr_name)''', raises=True)
    nb.md("## Эксперимент 1\n\nЭто опечатка в имени переменной — очень частая причина "
          "`NameError` у новичков. Найдите опечатку и исправьте её.")
    nb.code('''user_name = "Cartesian"
print(usr_name)''', raises=True)
    nb.md("## Исправление")
    nb.code('''user_name = "Cartesian"
print(user_name)''')
    nb.md("## Задание ★ Базовая практика\n\nВ ячейке ниже есть та же ошибка — "
          "переменная называется `score`, а выводится `scroe`. Исправьте опечатку, "
          "ничего больше не меняя.")
    nb.code('''score = 100
print(scroe)''', raises=True)
    nb.md("## Решение задания")
    nb.code('''score = 100
print(score)''')
    nb.write(OUT_DIR / "03-08-traceback-nameerror.ipynb")
    print(f"Записано: 03-08-traceback-nameerror.ipynb ({len(nb)} ячеек)")


def build_09_traceback_syntaxerror() -> None:
    nb = NotebookBuilder()
    nb.md("""\
# 03-09 · Прочитайте traceback: SyntaxError

Практика к разделу [«Типичные проблемы и отладка»](../../site/chapters/glava-03/03-14-debug-laboratorii.html).""")
    nb.md("## Цель\n\nНаучиться находить пропущенную скобку или кавычку по сообщению "
          "`SyntaxError`.")
    nb.md("## Что нужно знать\n\n`SyntaxError` означает: Python не смог даже понять "
          "структуру кода — до выполнения дело не дошло вовсе.")
    nb.md("## Краткое напоминание\n\n`SyntaxError` часто указывает не на саму "
          "проблемную скобку/кавычку, а на место, где Python «сдался» — иногда это "
          "следующая строка. Полезно перечитать всю строку целиком, а не только "
          "указанный символ.")
    nb.md("## Разминка\n\nПрежде чем смотреть на ошибку, запустите одну рабочую ячейку — "
          "чтобы Python-среда точно была готова.")
    nb.code('''print("Готовы искать ошибки!")''')
    nb.md("## Рабочий пример\n\nЗдесь не хватает закрывающей скобки.")
    nb.code('''print("Привет, Python!"''', raises=True)
    nb.md("## Исправление")
    nb.code('''print("Привет, Python!")''')
    nb.md("## Эксперимент 1\n\nА здесь не хватает закрывающей кавычки. Найдите и "
          "исправьте.")
    nb.code('''print("Привет, Python!)''', raises=True)
    nb.md("## Решение эксперимента 1")
    nb.code('''print("Привет, Python!")''')
    nb.md("## Задание ★ Базовая практика\n\nИсправьте обе ошибки в строке ниже — "
          "недостающую скобку И недостающую кавычку — и запустите без ошибок.")
    nb.code('''print("Готово!''', raises=True)
    nb.md("## Решение задания")
    nb.code('''print("Готово!")''')
    nb.write(OUT_DIR / "03-09-traceback-syntaxerror.ipynb")
    print(f"Записано: 03-09-traceback-syntaxerror.ipynb ({len(nb)} ячеек)")


def build_10_mini_project() -> None:
    nb = NotebookBuilder()
    nb.md("""\
# 03-10 · Мини-проект: Python-визитка

Практика к разделу [«Первый маленький проект»](../../site/chapters/glava-03/03-17-mini-proekt-i-itogi.html).""")
    nb.md("## Цель\n\nСобрать вместе всё, что уже знакомо — `input()`, имена, "
          "`print()` с `sep`/`end` — в одну маленькую цельную программу.")
    nb.md("## Что нужно знать\n\nВсе нужные инструменты уже пройдены в этой главе: "
          "`input()`, присваивание, `print()`.")
    nb.md("## Краткое напоминание\n\nДля надёжной проверки в примере ниже имена заданы "
          "заранее, как если бы пользователь их уже ввёл — в собственном проекте "
          "замените это на настоящие `input()`.")
    nb.md("## Рабочий пример")
    nb.code('''name = "Анна"
city = "Варшава"
goal = "игру"

print("========================")
print(" МОЯ PYTHON-КАРТОЧКА")
print("========================")
print("Имя:", name)
print("Город:", city)
print("Хочу создать:", goal)
print("========================")''')
    nb.md("## Эксперимент 1\n\nТеперь введите свои собственные данные через `input()`.")
    nb.code('''name = input("Ваше имя: ")
city = input("Ваш город: ")
goal = input("Что вы хотите создать на Python? ")

print("========================")
print(" МОЯ PYTHON-КАРТОЧКА")
print("========================")
print("Имя:", name)
print("Город:", city)
print("Хочу создать:", goal)
print("========================")''')
    nb.md("## Задание ★ Базовая практика\n\nДобавьте в визитку четвёртую строку — "
          "любимый язык программирования (можно захардкодить `\"Python\"`, а можно "
          "спросить через `input()`).")
    nb.code('''name = "Анна"
city = "Варшава"
goal = "игру"
language = "Python"

print("========================")
print(" МОЯ PYTHON-КАРТОЧКА")
print("========================")
print("Имя:", name)
print("Город:", city)
print("Хочу создать:", goal)
print("Любимый язык:", language)
print("========================")''')
    nb.md("## Самостоятельная практика ★★ · CHALLENGE\n\nПопробуйте своё оформление: "
          "другая рамка (например, из `*` вместо `=`), выравнивание через `sep`, или "
          "разделитель `—` вместо `:`.")
    nb.code('''name = "Анна"
print("*" * 24)
print("Имя", name, sep=" — ")
print("*" * 24)''')
    nb.write(OUT_DIR / "03-10-mini-proekt.ipynb")
    print(f"Записано: 03-10-mini-proekt.ipynb ({len(nb)} ячеек)")


if __name__ == "__main__":
    # NOTE: build_01..build_04 are kept above only as a historical record of
    # how 03-01..03-04 were first produced. Do NOT call them here — nbformat
    # assigns a fresh random cell id on every new_code_cell()/new_markdown_cell()
    # call, so re-running them regenerates different ids and silently breaks
    # the existing graders in site/practice/graders/03-0{1..4}.py, which
    # reference the original ids by exact string. Only 03-05..03-10 (added in
    # the Chapter 3 curriculum-v2 expansion) are safe to regenerate here.
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    build_05_repl_predict()
    build_06_input()
    build_07_names()
    build_08_traceback_nameerror()
    build_09_traceback_syntaxerror()
    build_10_mini_project()
