#!/usr/bin/env python3
"""Строит notebooks/chapter-01/01-01-dobro-pozhalovat.ipynb."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from notebook_lib import NotebookBuilder

ROOT = Path(__file__).resolve().parent.parent
OUT = ROOT / "notebooks" / "chapter-01" / "01-01-dobro-pozhalovat.ipynb"

nb = NotebookBuilder()

nb.md("""\
# 01-01 · Добро пожаловать в Python

Практика к главе 1 книги «Python с нуля» (Cartesian School). Никакой установки не нужно —
это ваш самый первый контакт с работающим кодом.""")

nb.md("""\
## Цель

После практики вы сможете:

- запустить ячейку с `print(...)`, изменить данные и предсказать новый вывод;
- создать имя, связать его со значением и объяснить повторное присваивание;
- распознать `NameError` и `SyntaxError`, исправить причину и подтвердить исправление запуском.""")

nb.md("""\
## Что нужно знать

Ничего. Правда. Если вы никогда раньше не запускали код — это отличное место начать.""")

nb.md("""\
## Краткое напоминание

Команда `print(...)` выводит на экран переданные ей значения. Строковый литерал записывают
в одинарных `'...'` или двойных `"..."` кавычках; выбранный вид кавычек не меняет тип строки.""")

nb.md("## Рабочий пример")

nb.code('''print("Привет, мир!")''')

nb.md("""\
## Эксперимент 1

Замените текст внутри кавычек на своё собственное приветствие и запустите ячейку снова.""")

nb.code('''print("Привет, Cartesian School!")''')

nb.md("""\
## Эксперимент 2

`print()` умеет выводить не только текст. Попробуйте передать ему число или даже пример
арифметики — Python сам всё посчитает.""")

nb.code('''print(2024 - 1991)
print(7 * 6)''')

nb.md("""\
## Типичная ошибка

Если убрать кавычки, Python решит, что вы обращаетесь к чему-то по имени — переменной или
функции, — а не к обычному тексту. Такого имени не существует, и Python сообщит об ошибке
`NameError`.""")

nb.code(
    '''# ошибка: текст без кавычек
print(Привет)''',
    raises=True,
)

nb.md("""\
## Исправление

Достаточно вернуть кавычки — тогда Python понимает, что это обычный текст, а не имя
переменной.""")

nb.code('''print("Привет")''')

nb.md("""\
## Задание ★ Базовая практика

Выведите на экран три строки: своё имя, свой любимый цвет и число, которое вам нравится.
Каждая строка — отдельный вызов `print()`.""")

nb.code('''print("Cartesian School")
print("Электрик-синий")
print(14)''', cell_id="3ca309a1")

nb.md("""\
## Самостоятельная практика

`print()` можно вызывать сколько угодно раз подряд — а значит, из символов на экране можно
собрать простую картинку. Попробуйте нарисовать текстом что-нибудь своё: домик, ёлочку,
смайлик — что угодно из символов клавиатуры.""")

nb.code('''print("  /\\\\  ")
print(" /  \\\\ ")
print("/____\\\\")''', cell_id="e1a23aa4")

nb.md("""\
## Дополнительная задача ★★★

`print()` может принять сразу несколько значений через запятую — тогда они выводятся в одну
строку через пробел. Выведите одной командой `print()` три разных значения: текст, число и
результат арифметического выражения.""")

nb.code('''print("Результат:", 6, 7 * 6)''', cell_id="ea50acd2")

nb.md("""\
## Имя и значение: управляемый эксперимент

Сначала предскажите обе строки вывода. Затем запустите ячейку и объясните результат словами:
имя `name` сначала связано с одним строковым значением, а затем — с другим.""")

nb.code('''name = "Анна"
print(name)
name = "Олег"
print(name)''')

nb.md("""\
## Самостоятельное задание: повторное присваивание

Создайте имя `course`, связанное со строкой `"Python"`, и выведите его. Затем свяжите то же
имя с другой строкой и выведите снова. Не копируйте пример выше: выберите собственное второе
значение. Успех — две разные непустые строки и объяснение, что изменилось связывание имени,
а не содержимое «коробки».""")

nb.code('''course = "Python"
print(course)
course = "Python 3.14"
print(course)''', cell_id="chapter01-variable-task")

nb.md("""\
## Debug Lab: синтаксическая ошибка

В следующей ячейке пропущена закрывающая скобка. Запустите её и найдите слово `SyntaxError`
в диагностике. Это ошибка разбора до выполнения, поэтому обычного заголовка traceback может
не быть. После наблюдения ошибки исправьте код в следующей ячейке.""")

nb.code('''print("Python помогает читать ошибки"''', raises=True)

nb.code('''print("Python помогает читать ошибки")''', cell_id="chapter01-syntax-fix")

nb.md("""\
## Заглянем внутрь: type()

У каждого значения в Python есть тип. Функция `type()` позволяет спросить реализацию Python,
к какому типу относится конкретное значение.""")

nb.code('''print(type("Привет"))
print(type(42))
print(type(3.14))''')

nb.md("""\
## Спросим сам Python: help()

Функция `help()` показывает встроенную справку, доступную в установленной реализации. Она
полезна рядом с кодом, но не обязана дословно совпадать с более полной и версионированной
страницей docs.python.org.""")

nb.code('''help(print)''')

nb.md("""\
## Пасхалка: дзен Python

У Python есть список афористичных принципов — «дзен Python», написанный Тимом Питерсом.
Открыть его можно одной строкой:""")

nb.code('''import this''')

nb.md("""\
## Итог без подсказки

До повторного запуска ответьте вслух или письменно:

1. Чем строковый литерал отличается от имени?
2. Что произойдёт после повторного присваивания одному имени?
3. С какой строки вы начнёте чтение `NameError` или `SyntaxError`?

Затем измените одно значение в самостоятельном задании, предскажите вывод и проверьте себя.""")

nb.write(OUT)
print(f"Записано: {OUT} ({len(nb)} ячеек)")
