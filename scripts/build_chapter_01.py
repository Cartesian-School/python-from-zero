#!/usr/bin/env python3
"""Строит Главу 1: «А вы знали?» (site/chapters/glava-01/).

Curriculum v2: расширенное, современное и визуально насыщенное введение —
что такое программирование, откуда взялся Python, как устроена его
экосистема (python.org, документация, PyPI, PSF/PEP), первые переменные и
первый опыт отладки. Существующие маршруты (index.html, 01-01/01-02/01-03,
практика 01-01) сохранены без изменений в URL — новый материал добавлен как
новые страницы (01-04..01-07) плюс расширение содержимого существующих.
"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from site_lib import (
    ChapterSectionLink,
    NavItem,
    PageNav,
    SidebarGroup,
    branch_diagram,
    callout,
    code_block,
    exercise,
    flow_diagram,
    image_figure,
    name_value_diagram,
    practice_card,
    render_chapter_opener,
    render_page,
    summary_box,
    timeline_diagram,
)

ROOT = Path(__file__).resolve().parent.parent
OUT_DIR = ROOT / "site" / "chapters" / "glava-01"

PAGES = [
    ("index.html", "Приступаем"),
    ("01-01-chto-takoe-programmirovanie.html", "Что такое программирование?"),
    ("01-04-istoriya-python.html", "История Python"),
    ("01-05-sajt-dokumentaciya-pypi.html", "python.org, документация и PyPI"),
    ("01-06-soobschestvo-i-filosofiya.html", "Сообщество и философия Python"),
    ("01-02-python-eto-veselo.html", "Python — это весело!"),
    ("01-07-peremennye-i-oshibki.html", "Первые переменные и первые ошибки"),
    ("01-03-kak-poluchit-maksimum.html", "Как получить максимум от книги"),
]


def sidebar(active_href: str) -> list[SidebarGroup]:
    items = [NavItem(title, href) for href, title in PAGES]
    for it in items:
        it.active = it.href == active_href
    return [
        SidebarGroup("Глава 1 · А вы знали?", items),
        SidebarGroup("Практика", [NavItem("[[icon:practice]] 01-01: Практика", "../../practice/01-01/index.html")]),
    ]


def write(name: str, html_out: str) -> None:
    path = OUT_DIR / name
    path.write_text(html_out, encoding="utf-8")
    print(f"Записано: {path.relative_to(ROOT)}")


PY_WORDMARK = '../../assets/img/brand/python-logo.svg'


def learning_outcomes(*items: str) -> str:
    """Render observable outcomes at the start of a Chapter 1 unit."""

    return callout(
        "info",
        "[[icon:success]] После этого раздела вы сможете",
        "<ul>" + "".join(f"<li>{item}</li>" for item in items) + "</ul>",
    )


def self_check(question: str, answer: str) -> str:
    """Render retrieval practice with learner-controlled feedback."""

    return (
        exercise(2, "[[icon:practice]] Проверьте себя", question)
        + '<details class="callout callout-tip"><summary><strong>Показать ответ и критерии</strong></summary>'
        + f"<p>{answer}</p></details>"
    )


def build_opener() -> None:
    out = render_chapter_opener(
        chapter_num=1,
        description="Первая глава книги «Python с нуля» — что такое программирование, откуда взялся "
        "Python, как устроена его экосистема и почему он отлично подходит для первого языка.",
        meta_items=["[[icon:timer]] ~70 минут", "[[icon:code]] без установки Python", "[[icon:practice]] 1 ноутбук практики"],
        intro_html=(
            learning_outcomes(
                "объяснить своими словами, как исходный код превращается в действия компьютера;",
                "найти официальный источник о Python и отличить стандартную библиотеку от стороннего пакета;",
                "запустить первый код, изменить его, прочитать простую ошибку и проверить исправление.",
            )
            + self_check(
                "До чтения запишите: что, по-вашему, происходит между нажатием «Выполнить» и результатом на экране? "
                "После главы вернитесь к ответу и исправьте его не более чем в трёх предложениях.",
                "В итоговом ответе должны появиться исходный код, реализация Python (в этой книге — CPython/Pyodide) "
                "и выполнение инструкций. Не требуется заранее знать байт-код: его роль объяснит раздел 1.1.",
            )
        ),
        sections=[
            ChapterSectionLink("1.1", "Что такое программирование?", "01-01-chto-takoe-programmirovanie.html"),
            ChapterSectionLink("", "Алгоритм, инструкция, выражение", "01-01-chto-takoe-programmirovanie.html#algoritm-i-instrukcii"),
            ChapterSectionLink("", "Интерпретатор и компилятор", "01-01-chto-takoe-programmirovanie.html#interpretator-i-kompilyator"),
            ChapterSectionLink("", "Почему вашим детям стоит научиться программировать?", "01-01-chto-takoe-programmirovanie.html#deti"),
            ChapterSectionLink("", "Почему Python?", "01-01-chto-takoe-programmirovanie.html#pochemu-python"),
            ChapterSectionLink("1.2", "История Python", "01-04-istoriya-python.html"),
            ChapterSectionLink("1.3", "python.org, документация и PyPI", "01-05-sajt-dokumentaciya-pypi.html"),
            ChapterSectionLink("1.4", "Сообщество и философия Python", "01-06-soobschestvo-i-filosofiya.html"),
            ChapterSectionLink("1.5", "Python — это весело!", "01-02-python-eto-veselo.html"),
            ChapterSectionLink("", "Где применяется Python", "01-02-python-eto-veselo.html#gde-primenyaetsya"),
            ChapterSectionLink("1.6", "Первые переменные и первые ошибки", "01-07-peremennye-i-oshibki.html"),
            ChapterSectionLink("1.7", "Как получить максимум от этой книги", "01-03-kak-poluchit-maksimum.html"),
            ChapterSectionLink("", "Итоги", "01-03-kak-poluchit-maksimum.html#itogi"),
        ],
    )
    write("index.html", out)


def build_01() -> None:
    layer2_flow = flow_diagram(
        [
            ("Исходный код", ".py файл, текст"),
            ("Байт-код", "компактные инструкции для VM"),
            ("Python VM", "выполняет байт-код"),
        ],
        caption="Уровень 2: что на самом деле делает CPython внутри",
    )

    body = f"""
{learning_outcomes(
        "различить программу, алгоритм, инструкцию и выражение на простом примере;",
        "описать упрощённый и уточнённый путь выполнения программы в CPython;",
        "обосновать, почему Python подходит для первого знакомства с программированием.",
    )}
    <h2 id="chto-takoe-programmirovanie">Что такое программирование?</h2>
    <p>Компьютер невероятно быстрый, но абсолютно несамостоятельный. Сам по себе он не умеет
    вообще ничего — ни открыть игру, ни показать фотографию, ни посчитать сдачу в магазине. Всё,
    что делает компьютер, он делает потому, что кто-то заранее написал для него точную
    последовательность команд. Эта последовательность и называется <strong>программой</strong>,
    а процесс её написания — <strong>программированием</strong>.</p>
    <p>Текст программы, который пишет человек, называется <strong>исходным кодом</strong>
    (source code). Компьютер не понимает исходный код напрямую — между вашим текстом и
    процессором стоит специальная программа-переводчик. Для Python она называется
    <strong>интерпретатором</strong>: он читает ваш код и превращает его в действия,
    которые понимает компьютер, — примерно как синхронный переводчик на переговорах.</p>

{flow_diagram([("Идея", "человек хочет что-то сделать"), ("Python-код", "точная запись идеи"), ("Интерпретатор", "переводит код в действия"), ("Компьютер", "выполняет действия")], caption="От идеи до результата: путь программы")}

{callout(
        "info",
        "[[icon:experiment]] Что происходит",
        "Когда вы нажимаете «Выполнить», происходит именно эта цепочка: ваш текст → интерпретатор "
        "Python → реальные действия компьютера (например, вывод текста на экран).",
    )}

    <h2 id="algoritm-i-instrukcii">Алгоритм, инструкция, выражение</h2>
    <p>Несколько слов, которые будут встречаться в каждой главе этой книги — полезно один раз
    договориться, что они означают:</p>
    <ul>
      <li><strong>Алгоритм</strong> — точный порядок шагов для решения задачи, ещё до того как он
        записан на каком-либо языке программирования. Рецепт борща — тоже алгоритм: точная
        последовательность действий, которая всегда приводит к одному результату.</li>
      <li><strong>Инструкция</strong> (statement) — одна законченная команда программе, например
        «выведи текст на экран» или «сохрани число в память».</li>
      <li><strong>Выражение</strong> (expression) — часть кода, у которой есть значение, например
        <code class="inline">2 + 2</code> или <code class="inline">"Привет" + "!"</code>. Выражения
        часто находятся внутри инструкций.</li>
      <li><strong>Программа</strong> — упорядоченный набор инструкций на языке программирования,
        реализующий один или несколько алгоритмов.</li>
      <li><strong>Машинный код</strong> — последовательность чисел, которую напрямую выполняет
        процессор. Люди почти никогда не пишут его руками — именно для этого и нужны языки вроде
        Python и переводчики, которые превращают удобный для человека код в машинный.</li>
    </ul>

    <h2 id="interpretator-i-kompilyator">Интерпретатор и компилятор</h2>
    <p><strong>Уровень 1 (достаточно для начала).</strong> Python читает ваш код и сразу
    выполняет его — команда за командой, сверху вниз. Программу-переводчика, которая это делает,
    называют <strong>интерпретатором</strong>.</p>
    <p>Для сравнения: у некоторых языков (например, C или Rust) есть отдельный шаг —
    <strong>компилятор</strong> заранее переводит весь исходный код целиком в машинный код, и
    только потом готовую программу запускают. Python устроен иначе — переводить и выполнять
    происходит как бы одновременно, поэтому его называют <em>интерпретируемым</em> языком.</p>

{callout(
        "warning",
        "[[icon:warning]] Распространённая неточность",
        "Говорить «Python вообще никогда не компилируется» — не совсем точно. На самом деле "
        "происходит немного больше — см. уровень 2 ниже. Для начала уровня 1 полностью достаточно, "
        "чтобы писать и понимать код.",
    )}

    <p><strong>Уровень 2 (если интересно, что происходит на самом деле).</strong> Официальная
    («эталонная») реализация Python, CPython, на самом деле сначала незаметно компилирует ваш
    исходный код в промежуточную форму — <strong>байт-код</strong> (компактные инструкции,
    непонятные человеку, но быстрые для выполнения), а затем выполняет этот байт-код с помощью
    Python VM (виртуальной машины). Всё это происходит автоматически и незаметно для вас —
    просто теперь вы знаете, что «интерпретируемый» не значит «без единого шага компиляции».</p>

{layer2_flow}

    <p>Подробнее об устройстве CPython — в разделе «Сообщество и философия Python» этой же
    главы.</p>

    <h2 id="deti">Почему вашим детям стоит научиться программировать?</h2>
    <p>Программирование — не заявка на профессию «программист», а способ мышления, который
    пригождается в любой области. Это касается и взрослых, которые садятся за первую программу
    сами. Три причины, почему этому стоит учиться в любом возрасте:</p>
    <ul>
      <li><strong>Ошибка — это нормально.</strong> Программа почти никогда не работает с первого
        раза, и это не повод расстраиваться — это часть процесса. Программирование учит спокойно
        находить причину и пробовать снова, вместо того чтобы бояться ошибиться.</li>
      <li><strong>Идея превращается в результат.</strong> Мысль «а что если сделать вот так»
        можно проверить за несколько минут, написав и запустив код, — не нужно ничьё разрешение.</li>
      <li><strong>Это переносимый навык.</strong> Умение разложить сложную задачу на простые шаги
        пригождается далеко за пределами программирования — в учёбе, работе и повседневных
        задачах.</li>
    </ul>

    <h2 id="pochemu-python">Почему Python?</h2>
    <p>Языков программирования — сотни. Python выбран для первого языка по трём практическим
    причинам:</p>
    <ul>
      <li><strong>Читается почти как обычный текст.</strong> Строка
        <code class="inline">if age &gt;= 18:</code> понятна без перевода. Меньше отвлечённого
        синтаксиса — больше внимания на саму идею.</li>
      <li><strong>Один язык — очень разные результаты.</strong> На Python пишут игры, рисуют
        графику, собирают настольные и веб-приложения, обучают модели искусственного интеллекта.
        Всё, о чём вы прочитаете в этой книге, — на одном и том же языке.</li>
      <li><strong>Огромное сообщество.</strong> Если у вас возникнет вопрос — скорее всего, кто-то
        уже задавал его и получил ответ. Готовых инструментов (их называют библиотеками или
        пакетами) для Python написано огромное количество — подробнее об этом чуть позже в этой
        же главе.</li>
    </ul>
{self_check(
        "Распределите четыре примера по понятиям «алгоритм», «инструкция», «выражение», «программа»: "
        "<code class=\"inline\">2 + 2</code>; <code class=\"inline\">print(2 + 2)</code>; рецепт из пяти шагов; "
        "файл из нескольких команд, решающих задачу. Затем объясните путь этого файла в CPython.",
        "<code class=\"inline\">2 + 2</code> — выражение; <code class=\"inline\">print(2 + 2)</code> — "
        "инструкция, внутри которой есть выражение; рецепт — алгоритм; файл команд — программа. CPython компилирует "
        "исходный код в байт-код для своей виртуальной машины, после чего выполняет его; это не превращает обычный "
        "<code class=\"inline\">.py</code>-файл заранее в самостоятельный машинный исполняемый файл.",
    )}
    """

    out = render_page(
        page_title="Что такое программирование?",
        description="Что такое программирование, алгоритм, интерпретатор и компилятор — и почему "
        "Python хорошо подходит для первого языка программирования.",
        depth=2,
        breadcrumb=[("Python с нуля", "../../index.html"), ("Глава 1", "index.html"), ("Что такое программирование?", "")],
        kicker="Глава 1 · А вы знали?",
        h1="Что такое программирование?",
        lede="Прежде чем писать код, полезно понять, что вообще происходит, когда компьютер "
        "«выполняет программу» — и почему для первого языка мы выбрали именно Python.",
        body_html=body,
        sidebar_groups=sidebar("01-01-chto-takoe-programmirovanie.html"),
        nav=PageNav(prev_href="index.html", prev_label="Приступаем", next_href="01-04-istoriya-python.html", next_label="История Python"),
    )
    write("01-01-chto-takoe-programmirovanie.html", out)


def build_04_history() -> None:
    timeline = timeline_diagram(
        [
            ("CWI, Нидерланды", "исследовательский институт, где рождается идея"),
            ("Язык ABC", "предшественник Python — простой, но со своими ограничениями"),
            ("Конец 1989", "Гвидо ван Россум начинает Python на рождественских каникулах"),
            ("1991 · Python 0.9.0", "первый публичный релиз"),
            ("1994 · Python 1.0", "первая полноценная версия"),
            ("2000 · Python 2.0", "списковые включения, сборщик мусора с циклами"),
            ("2001 · создана PSF", "Python Software Foundation — некоммерческая организация"),
            ("2008 · Python 3.0", "осознанно несовместимые улучшения языка"),
            ("2020 · конец Python 2", "официальная поддержка Python 2 прекращена"),
            ("Сегодня · Python 3.14", "актуальный стабильный релиз, база этой книги"),
        ],
        caption="Краткая хронология Python",
    )

    body = f"""
{learning_outcomes(
        "расположить ключевые события истории Python от ABC и 1989 года до ветки 3.x;",
        "объяснить, почему Python 3 потребовал намеренно несовместимых изменений;",
        "отличить исторический факт от удобной легенды и назвать официальный источник проверки.",
    )}
    <div style="display:flex;align-items:center;gap:14px;margin-bottom:8px">
      <img src="{PY_WORDMARK}" alt="Python" height="40" style="display:block" />
    </div>
    <h2 id="do-python">До Python: CWI и язык ABC</h2>
    <p>Python появился не на пустом месте. В 1980-х годах в нидерландском исследовательском
    институте <strong>CWI</strong> (Centrum Wiskunde &amp; Informatica, «Центр математики и
    информатики») разрабатывался язык <strong>ABC</strong> — он должен был быть простым и
    удобным для обучения программированию непрофессионалов. У ABC были хорошие идеи (например,
    отступы вместо фигурных скобок), но были и серьёзные ограничения — он не был предназначен
    для больших «настоящих» программ и плохо расширялся.</p>

    <h2 id="rozhdenie">Рождение Python: 1989–1991</h2>
    <p>Один из разработчиков ABC, нидерландский программист <strong>Гвидо ван Россум</strong>
    (Guido van Rossum), хорошо знал сильные и слабые стороны ABC. В конце декабря 1989 года, во
    время рождественских каникул, он начал писать новый язык — как хобби-проект, чтобы занять
    себя на праздниках, и как способ сохранить понравившиеся идеи ABC, избавившись от его
    ограничений.</p>

<div style="max-width:280px">
{image_figure(
        '../../assets/img/people/guido-van-rossum.jpg',
        "Гвидо ван Россум на конференции PyCon US",
        "Гвидо ван Россум, создатель Python, на PyCon US 2024. Фото: Kushal Das, "
        "лицензия CC BY-SA 4.0 (изображение обрезано и сжато для сайта).",
    )}
    </div>

    <p>Первый публичный релиз, <strong>Python 0.9.0</strong>, вышел в феврале 1991 года. В нём
    уже были функции, обработка исключений, классы с наследованием и встроенные типы данных
    вроде списков и словарей — многое из того, что вы будете использовать в этой книге.</p>

{callout(
        "tip",
        "[[icon:idea]] Идея",
        "Python начинался как личный проект одного человека для собственного удовольствия. Не "
        "обязательно с самого начала «менять индустрию» — иногда достаточно решить свою "
        "собственную небольшую задачу.",
    )}

    <h2 id="nazvanie">Откуда взялось название «Python»</h2>
    <p>Несмотря на логотип со змеями, язык назван вовсе не в честь пресмыкающегося. Гвидо ван
    Россум — большой поклонник британского комедийного шоу <strong>«Летающий цирк Монти Пайтона»</strong>
    (Monty Python's Flying Circus), и ему хотелось короткое, немного необычное и запоминающееся
    имя. Змеиная тема логотипа появилась уже позже, как визуальная ассоциация со словом «Python».</p>

    <h2 id="python-1-i-2">Python 1.x и Python 2.0</h2>
    <p>Версия <strong>Python 1.0</strong> вышла в январе 1994 года. Язык продолжал расти:
    появлялись новые модули стандартной библиотеки, росло сообщество разработчиков.</p>
    <p>В октябре 2000 года вышел <strong>Python 2.0</strong>, принёсший важные для языка вещи —
    например, списковые включения (list comprehensions) и полноценный сборщик мусора,
    умеющий находить и освобождать память даже у объектов, ссылающихся друг на друга по кругу.</p>
    <p>В 2001 году была создана <strong>Python Software Foundation (PSF)</strong> — некоммерческая
    организация, которая владеет интеллектуальной собственностью проекта и поддерживает его
    инфраструктуру и сообщество. Техническое управление языком устроено отдельно: решения о
    развитии принимаются через открытый процесс PEP и модель управления, описанную в PEP 13.
    Подробнее об этом — в следующем разделе главы.</p>

    <h2 id="python-3">Python 3: зачем понадобилась несовместимая версия</h2>
    <p>К середине 2000-х в языке накопились особенности, которые нельзя было исправить, не
    сломав часть старого кода, — например, то, как Python 2 обрабатывал текст и байты, было
    источником постоянной путаницы, особенно при работе с не-английскими языками (в том числе
    русским). Разработчики Python сознательно пошли на выпуск <strong>Python 3.0</strong>
    (декабрь 2008) — версии, которая местами <em>намеренно несовместима</em> со старым кодом
    ради того, чтобы исправить эти накопившиеся проблемы: единая модель текста (Unicode
    по умолчанию), <code class="inline">print()</code> стала обычной функцией, а не особым
    словом языка, целочисленное деление стало более предсказуемым, и многое другое.</p>

{callout(
        "info",
        "[[icon:note]] Официальный источник",
        "Полный и точный список причин перехода на Python 3 — в официальном документе "
        "PEP 3100 «Miscellaneous Python 3.0 Plans» и разделе «What's New» на docs.python.org.",
    )}

    <p>Переход занял намного дольше, чем ожидалось: сообществу требовалось время, чтобы
    переписать старый код и библиотеки. Официальная поддержка <strong>Python 2 закончилась
    1 января 2020 года</strong> — с этого момента для Python 2 больше не выпускают даже
    исправления критических уязвимостей.</p>

    <h2 id="sovremennyj-python">Путь к современному Python и версия 3.14</h2>
    <p>После 2008 года Python продолжил развиваться заметными, но уже совместимыми шагами:
    f-строки для удобного форматирования текста (3.6), оператор «морж» <code class="inline">:=</code>
    (3.8), более понятные и точные сообщения об ошибках и структурное сопоставление с образцом
    (3.10), заметный прирост скорости выполнения в нескольких последних релизах. Именно это
    непрерывное, но осторожное развитие и определяет то, что сегодня называют «современным
    Python».</p>
    <p>Эта книга ориентирована на стабильную ветку <strong>Python 3.14</strong>. Номер исправляющего
    релиза внутри ветки 3.14 со временем меняется, поэтому точную доступную версию всегда следует
    проверять на python.org; примеры курса не зависят от конкретного номера 3.14.x.</p>

{timeline}
{self_check(
        "Без подсказки расположите по времени: Python 0.9.0, Python 3.0, окончание поддержки Python 2, "
        "начало работы над Python. Затем одним предложением объясните, зачем понадобился Python 3.",
        "Конец 1989 года → Python 0.9.0 (1991) → Python 3.0 (2008) → окончание поддержки Python 2 "
        "(2020). Python 3 позволил исправить накопившиеся модели и интерфейсы, которые нельзя было безопасно "
        "изменить, сохранив полную совместимость со старым кодом.",
    )}"""

    out = render_page(
        page_title="История Python",
        description="От языка ABC и исследовательского института CWI до Python 3.14: почему и как "
        "появился и развивался Python.",
        depth=2,
        breadcrumb=[("Python с нуля", "../../index.html"), ("Глава 1", "index.html"), ("История Python", "")],
        kicker="Глава 1 · А вы знали?",
        h1="История Python",
        lede="Python появился не случайно и не сразу — короткая, но настоящая история о том, "
        "почему язык устроен именно так, как устроен сегодня.",
        body_html=body,
        sidebar_groups=sidebar("01-04-istoriya-python.html"),
        nav=PageNav(prev_href="01-01-chto-takoe-programmirovanie.html", prev_label="Что такое программирование?", next_href="01-05-sajt-dokumentaciya-pypi.html", next_label="python.org, документация и PyPI"),
    )
    write("01-04-istoriya-python.html", out)


def build_05_ecosystem_site() -> None:
    pypi_diagram = branch_diagram(
        "Python",
        [
            ("Стандартная библиотека", "уже встроена, ничего ставить не нужно"),
            ("Сторонние пакеты", "устанавливаются отдельно, обычно с PyPI"),
        ],
        caption="Две части того, чем можно пользоваться в Python",
    )

    body = f"""
{learning_outcomes(
        "выбрать между python.org, docs.python.org, PyPI и Python Packaging User Guide для конкретного вопроса;",
        "отличить стандартную библиотеку от стороннего пакета;",
        "объяснить, почему имя пакета и источник установки нужно проверять до запуска команды.",
    )}
    <div style="display:flex;align-items:center;gap:14px;margin-bottom:8px">
      <img src="{PY_WORDMARK}" alt="Python" height="40" style="display:block" />
    </div>
    <h2 id="python-org">python.org — официальный сайт</h2>
    <p><strong>python.org</strong> — официальный сайт языка Python, которым управляет Python
    Software Foundation. Это первое место, где стоит искать официальную информацию: как
    установить Python, где почитать документацию, что нового в последнем релизе.</p>

{image_figure(
        '../../assets/img/screenshots/python-org-homepage.jpg',
        "Главная страница python.org с разделами Get Started, Download, Docs и Jobs",
        "python.org — официальный сайт Python. Обратите внимание на верхнее меню (About, "
        "Downloads, Documentation, Community) и блок «Download» с актуальной версией.",
    )}

    <p>На главной странице стоит запомнить четыре раздела верхнего меню:</p>
    <ul>
      <li><strong>Downloads</strong> — официальные выпуски: установщики для Windows и macOS,
        исходные архивы и ссылки для других платформ. В Linux обычно используют менеджер
        пакетов дистрибутива; варианты для каждой ОС разберём в главе 2.</li>
      <li><strong>Documentation</strong> — ссылка на docs.python.org, официальную документацию
        (подробнее ниже).</li>
      <li><strong>Community</strong> — форумы, списки рассылки, локальные сообщества
        Python-разработчиков.</li>
      <li><strong>PSF</strong> (вкладка вверху страницы) — раздел о самой организации,
        Python Software Foundation.</li>
    </ul>

{callout(
        "warning",
        "[[icon:warning]] Типичная ошибка",
        "В интернете много сайтов с похожими названиями и «упрощённой установкой Python», "
        "которые на самом деле не имеют отношения к официальному проекту. Надёжный признак "
        "подлинности — домен <code class=\"inline\">python.org</code> в адресной строке "
        "браузера.",
    )}

    <h2 id="dokumentaciya">Официальная документация: docs.python.org</h2>
    <p>Документация — это не что-то, что читают только опытные разработчики. Хорошая
    документация — рабочий инструмент на каждый день, которым пользуются абсолютно все, включая
    авторов самого языка.</p>

{image_figure(
        '../../assets/img/screenshots/docs-python-org.jpg',
        "Главная страница docs.python.org 3.14 с разделами Tutorial, Library reference, Language reference",
        "docs.python.org — официальная документация. Три раздела в левой верхней части страницы "
        "стоит различать с самого начала.",
    )}

    <p>На странице документации сразу видно три разных раздела, и стоит понимать разницу между
    ними:</p>
    <ul>
      <li><strong>Tutorial</strong> — учебное введение в язык, написанное для тех, кто уже
        немного знаком с программированием. Хороший источник для повторения, но не замена этой
        книге для полного новичка.</li>
      <li><strong>Library Reference</strong> — подробное описание стандартной библиотеки: что
        умеет каждый встроенный модуль и функция. Именно сюда вы будете заглядывать чаще всего,
        когда захотите узнать, что ещё умеет, например, <code class="inline">print()</code>.</li>
      <li><strong>Language Reference</strong> — формальное, самое строгое описание правил самого
        языка. Обычно не нужен новичку — рассчитан на очень точные технические вопросы.</li>
    </ul>

{callout(
        "tip",
        "[[icon:idea]] Идея",
        "Пока не нужно понимать документацию целиком. Цель этого раздела — просто перестать "
        "бояться слова «документация» и один раз увидеть, как она устроена.",
    )}

    <h2 id="pypi">PyPI: сторонние пакеты Python</h2>
    <p>В Python уже встроено много полезного — это называется <strong>стандартной
    библиотекой</strong>. Но существует ещё огромное количество готовых инструментов,
    написанных другими разработчиками и опубликованных отдельно — их называют
    <strong>пакетами</strong> (или библиотеками). Они расширяют возможности Python далеко за
    пределы того, что есть «из коробки».</p>

{pypi_diagram}

    <p>Главное публичное хранилище таких пакетов называется <strong>PyPI</strong> — Python Package
    Index (pypi.org). По умолчанию <code class="inline">pip</code> ищет выпуски пакетов в PyPI,
    но его можно настроить на другой индекс или прямой источник. Поэтому команда установки сама
    по себе не доказывает происхождение и безопасность пакета.</p>

{image_figure(
        '../../assets/img/screenshots/pypi-org-homepage.jpg',
        "Главная страница pypi.org с полем поиска и статистикой количества проектов",
        "pypi.org — Python Package Index, каталог сторонних пакетов Python.",
    )}

{callout(
        "info",
        "[[icon:note]] Официальный источник",
        "Подробное практическое руководство по установке пакетов — packaging.python.org. Само "
        "практическое умение пользоваться <code class=\"inline\">pip</code> и виртуальными "
        "окружениями понадобится вам позже в этой книге — пока достаточно понимать саму идею "
        "экосистемы пакетов.",
    )}
{self_check(
        "Куда вы пойдёте в каждом случае: (1) скачать официальный Python; (2) уточнить параметры "
        "<code class=\"inline\">print()</code>; (3) проверить существование стороннего пакета; "
        "(4) научиться безопасно устанавливать пакеты? Объясните каждый выбор.",
        "(1) python.org; (2) Library Reference на docs.python.org; (3) pypi.org с проверкой владельцев, "
        "описания и ссылок проекта; (4) packaging.python.org. Language Reference нужен для строгих правил языка, "
        "а не как первая справка по параметрам встроенной функции.",
    )}"""

    out = render_page(
        page_title="python.org, документация и PyPI",
        description="Официальный сайт Python, docs.python.org и PyPI — как устроена экосистема "
        "Python и где искать официальную информацию.",
        depth=2,
        breadcrumb=[("Python с нуля", "../../index.html"), ("Глава 1", "index.html"), ("python.org, документация и PyPI", "")],
        kicker="Глава 1 · А вы знали?",
        h1="python.org, документация и PyPI",
        lede="Три места, с которыми полезно познакомиться в самом начале: официальный сайт, "
        "официальная документация и каталог сторонних пакетов.",
        body_html=body,
        sidebar_groups=sidebar("01-05-sajt-dokumentaciya-pypi.html"),
        nav=PageNav(prev_href="01-04-istoriya-python.html", prev_label="История Python", next_href="01-06-soobschestvo-i-filosofiya.html", next_label="Сообщество и философия Python"),
    )
    write("01-05-sajt-dokumentaciya-pypi.html", out)


def build_06_community_philosophy() -> None:
    impl_diagram = branch_diagram(
        "Python (язык)",
        [
            ("CPython", "эталонная реализация, используется в этой книге"),
            ("PyPy", "ускоренная реализация с JIT-компиляцией"),
            ("MicroPython", "для микроконтроллеров и встраиваемых систем"),
        ],
        caption="Python — это язык; у языка есть несколько реализаций",
    )

    body = f"""
{learning_outcomes(
        "различить обязанности PSF, процесса PEP и Steering Council;",
        "объяснить разницу между языком Python и одной из его реализаций;",
        "применить один принцип дзена Python к сравнению двух вариантов кода.",
    )}
    <h2 id="psf">Python Software Foundation (PSF)</h2>
    <p><strong>Python Software Foundation</strong> — некоммерческая организация в США,
    основанная в 2001 году. Ей принадлежат права на интеллектуальную собственность Python; она
    поддерживает проект и сообщество,
    организует конференции (например, ежегодную PyCon US) и финансирует инфраструктуру
    сообщества — включая сам сайт python.org и PyPI.</p>
    <p>PSF не продаёт Python и не выдаёт официальных сертификатов от имени языка. Она не
    подменяет техническое управление: Steering Council и участники процесса PEP принимают
    решения о языке по правилам, закреплённым в PEP 13.</p>

    <h2 id="pep">PEP: как Python принимает решения об изменениях</h2>
    <p>Многие существенные изменения языка, стандартной библиотеки и процессов проекта проходят
    через формальный
    процесс — <strong>PEP</strong> (Python Enhancement Proposal, «предложение по улучшению
    Python»). Это документ, в котором кто-то подробно описывает предлагаемое изменение,
    объясняет, зачем оно нужно, и фиксирует обсуждение и решение. Не каждая небольшая правка
    требует нового PEP.</p>
    <p>Все PEP пронумерованы и опубликованы на peps.python.org. Два PEP стоит знать по имени
    уже сейчас, просто чтобы узнавать их в будущем:</p>
    <ul>
      <li><strong>PEP 8</strong> — руководство по стилю оформления кода Python (как называть
        переменные, сколько оставлять отступов и пробелов). Мы вернёмся к нему подробно позже
        в этой книге.</li>
      <li><strong>PEP 20</strong> — он же «дзен Python», о котором ниже.</li>
    </ul>

    <h2 id="dzen-python">Дзен Python</h2>
    <p>У Python есть собственный короткий список принципов, объясняющих, каким язык старается
    быть. Его написал разработчик Тим Питерс, и увидеть его можно, выполнив в интерпретаторе
    Python одну строку:</p>
{code_block("dzen.py", "import this")}
    <p>Своими словами, а не дословной цитатой, идея сводится к нескольким вещам, которые
    Python ценит больше всего:</p>
    <ul>
      <li><strong>Читаемость важнее краткости.</strong> Понятный код лучше кода, который
        уместился в одну строку ценой ясности.</li>
      <li><strong>Явное лучше неявного.</strong> Код должен по возможности прямо показывать,
        что он делает, а не полагаться на скрытую «магию».</li>
      <li><strong>Простота лучше сложности — но не ценой возможностей.</strong> Если задача
        сложная, решение может быть сложным, но не сложнее, чем должно быть.</li>
    </ul>

{exercise(1, "[[icon:practice]] Попробуем", "Откройте практику этого раздела и выполните ячейку с "
      "<code class=\"inline\">import this</code>. Прочитайте вывод целиком — эти строки "
      "будут появляться в подсказках книги ещё не раз.")}

    <h2 id="cpython-i-drugie">CPython и другие реализации</h2>
    <p>Полезно различать две вещи: <strong>Python как язык</strong> (правила синтаксиса и
    поведения, которые описывает Language Reference) и <strong>конкретную программу, которая
    этот язык выполняет</strong> — её называют реализацией.</p>
    <p>Самая распространённая реализация — <strong>CPython</strong>, написанная на языке C. Её
    вы используете на протяжении всей этой книги, и её же скачивают с python.org по умолчанию.
    Существуют и другие реализации, созданные для особых задач:</p>

{impl_diagram}

    <ul>
      <li><strong>PyPy</strong> — реализация с JIT-компиляцией (Just-In-Time), которая на
        некоторых задачах выполняет код заметно быстрее CPython за счёт компиляции «на лету»
        во время выполнения.</li>
      <li><strong>MicroPython</strong> — компактная реализация Python для микроконтроллеров и
        встраиваемых устройств с очень ограниченной памятью — от роботов до умных датчиков.</li>
    </ul>

{callout(
        "tip",
        "[[icon:idea]] Идея",
        "Для этой книги достаточно знать одно: номер «Python 3.14.x» описывает совместимую ветку "
        "языка и реализации. Локальная установка с python.org обычно является CPython, а "
        "браузерная практика использует CPython, собранный для WebAssembly в составе Pyodide.",
    )}
{self_check(
        "Распределите роли: кто поддерживает инфраструктуру и права проекта; где оформляют и "
        "обсуждают существенное предложение; что непосредственно выполняет код. Затем объясните, "
        "почему фраза «Python написан на C» неточна.",
        "PSF поддерживает организацию, инфраструктуру и интеллектуальную собственность; PEP фиксирует "
        "предложение и решение; реализация, например CPython, выполняет код. На C написан CPython, но Python "
        "как язык задаётся спецификацией и имеет другие реализации, поэтому нельзя отождествлять язык с CPython.",
    )}"""

    out = render_page(
        page_title="Сообщество и философия Python",
        description="Python Software Foundation, PEP, дзен Python и разница между Python как "
        "языком и CPython как его основной реализацией.",
        depth=2,
        breadcrumb=[("Python с нуля", "../../index.html"), ("Глава 1", "index.html"), ("Сообщество и философия Python", "")],
        kicker="Глава 1 · А вы знали?",
        h1="Сообщество и философия Python",
        lede="Кто решает, каким становится Python, какими принципами он руководствуется — и "
        "почему «Python» и «CPython» не совсем одно и то же.",
        body_html=body,
        sidebar_groups=sidebar("01-06-soobschestvo-i-filosofiya.html"),
        nav=PageNav(prev_href="01-05-sajt-dokumentaciya-pypi.html", prev_label="python.org, документация и PyPI", next_href="01-02-python-eto-veselo.html", next_label="Python — это весело!"),
    )
    write("01-06-soobschestvo-i-filosofiya.html", out)


def build_02() -> None:
    peek_code = code_block(
        "podglyadyvaem_vpered.py",
        'import turtle\n\n'
        'artist = turtle.Turtle()\n'
        'for _ in range(4):\n'
        '    artist.forward(100)   # едем вперёд\n'
        '    artist.right(90)      # поворачиваем на 90°\n',
    )

    body = f"""
{learning_outcomes(
        "назвать несколько областей, где Python применяется профессионально;",
        "выбрать Python или другой класс инструментов для задачи и обосновать ограничение;",
        "запустить первую практику, изменить пример и проверить наблюдаемый результат.",
    )}
    <h2 id="igry">Игры!</h2>
    <p>В главах 17, 19 и 21 вы соберёте три настоящие игры целиком: «Крестики-нолики»,
    «Змейку» и космический шутер — от пустого окна до готовой игры с очками и правилами.
    Каждая из них строится маленькими понятными шагами, а не появляется одним большим куском
    кода.</p>

    <h2 id="grafika">Графика и анимация</h2>
    <p>Уже в главе 6 вы начнёте рисовать на экране с помощью модуля <code class="inline">turtle</code>
    — виртуальной черепашки с пером, которая двигается по вашим командам. Вот как выглядит код,
    рисующий квадрат — просто для примера, разбирать его подробно будем позже:</p>
{peek_code}
{callout(
        "tip",
        "Забегая вперёд",
        "Не пытайтесь понять этот код прямо сейчас — <code class=\"inline\">for</code> "
        "(цикл) появится только в главе 10. Здесь достаточно увидеть, что настоящий рисунок "
        "получается всего из нескольких строк.",
    )}

    <h2 id="veb-sajty">Веб-сайты</h2>
    <p>В главе 22 вы узнаете, как устроен веб — что делают HTML, CSS и JavaScript в браузере,
    какую роль играет Python на сервере, — и запустите собственный работающий сайт на Flask.</p>

    <h2 id="prilozhenija">Приложения</h2>
    <p>С помощью модуля <code class="inline">Tkinter</code> (главы 16–18) вы соберёте настоящие
    оконные приложения с кнопками и полями ввода — калькулятор чаевых и собственное приложение
    для рисования, — а не только программы, которые выводят текст в терминал.</p>

    <h2 id="gde-primenyaetsya">Где применяется Python в реальном мире</h2>
    <p>Игры, графика, приложения и сайты из этой книги — только часть картины. За пределами
    учебного курса Python используют в очень разных областях:</p>
    <ul>
      <li><strong>Автоматизация и скрипты</strong> — переименовать сотни файлов, обработать
        таблицу, запустить рутинную задачу по расписанию.</li>
      <li><strong>Веб-разработка</strong> — серверная часть крупных сайтов (Django, Flask и
        похожие инструменты).</li>
      <li><strong>Анализ данных и наука</strong> — обработка больших таблиц данных, статистика,
        научные вычисления (pandas, NumPy).</li>
      <li><strong>Искусственный интеллект и машинное обучение</strong> — обучение и запуск
        моделей (PyTorch, TensorFlow и другие).</li>
      <li><strong>Серверные и backend-сервисы</strong> — API, очереди задач, внутренние
        инструменты компаний.</li>
      <li><strong>Тестирование программ</strong> — автоматические тесты для другого софта,
        независимо от того, на каком языке он написан.</li>
      <li><strong>DevOps и инфраструктура</strong> — скрипты развёртывания, автоматизация
        серверов.</li>
      <li><strong>Образование</strong> — один из самых популярных первых языков программирования
        в мире, в том числе в этой книге.</li>
      <li><strong>Инструменты кибербезопасности</strong> — сканеры, автоматизация анализа
        уязвимостей.</li>
      <li><strong>Встраиваемые системы</strong> — MicroPython на микроконтроллерах и
        небольших устройствах.</li>
    </ul>

    <h2 id="gde-ne-luchshij-vybor">Где Python — не лучший выбор</h2>
    <p>Честности ради: Python не подходит одинаково хорошо для абсолютно всего. Несколько
    примеров, где обычно выбирают другие инструменты:</p>
    <ul>
      <li><strong>Системы жёсткого реального времени</strong> — там, где опоздание в доли
        миллисекунды недопустимо (например, некоторые встроенные системы управления), обычно
        выбирают более предсказуемые по времени выполнения языки.</li>
      <li><strong>Некоторые нативные мобильные приложения</strong> — для iOS и Android чаще
        используют инструменты, «родные» для этих платформ.</li>
      <li><strong>Низкоуровневая работа с ядром ОС и драйверами устройств</strong> — эта область
        обычно требует языков вроде C, дающих прямой контроль над памятью и железом.</li>
      <li><strong>Сильно ограниченные по памяти окружения</strong> — там, где даже MicroPython
        оказывается слишком «тяжёлым», выбирают C или ассемблер.</li>
    </ul>
    <p>Это нормально — ни один язык программирования не универсален. Знать сильные и слабые
    стороны инструмента — часть профессионального роста.</p>

{practice_card(
        "01-01",
        "Практика: первый запуск кода",
        "Интерактивный ноутбук прямо в браузере — Python 3.14 через Pyodide, без установки",
        "../../practice/01-01/index.html",
    )}
{self_check(
        "Для каждой задачи выберите Python или более подходящий класс инструментов и назовите причину: "
        "автоматизация имён 500 файлов; драйвер устройства; анализ таблицы; контур жёсткого реального времени. "
        "После ответа откройте практику 01-01 и измените хотя бы два значения в рабочем примере.",
        "Python хорошо подходит для автоматизации файлов и анализа данных благодаря стандартной библиотеке и "
        "экосистеме. Для драйвера обычно нужен системный язык с низкоуровневым доступом, а для жёсткого реального "
        "времени — среда с доказуемо предсказуемыми задержками. Успех практики подтверждается новым, заранее "
        "предсказанным выводом после изменения значений.",
    )}
    """

    out = render_page(
        page_title="Python — это весело!",
        description="Игры, графика, веб-сайты и приложения из этой книги — и честный обзор того, "
        "где Python применяется в реальном мире, а где не является лучшим выбором.",
        depth=2,
        breadcrumb=[("Python с нуля", "../../index.html"), ("Глава 1", "index.html"), ("Python — это весело!", "")],
        kicker="Глава 1 · А вы знали?",
        h1="Python — это весело!",
        lede="Короткая экскурсия по тому, что вы будете строить своими руками на страницах этой "
        "книги, — и честный взгляд на то, где Python применяется в реальном мире.",
        body_html=body,
        sidebar_groups=sidebar("01-02-python-eto-veselo.html"),
        nav=PageNav(prev_href="01-06-soobschestvo-i-filosofiya.html", prev_label="Сообщество и философия Python", next_href="01-07-peremennye-i-oshibki.html", next_label="Первые переменные и первые ошибки"),
    )
    write("01-02-python-eto-veselo.html", out)


def build_07_variables_errors() -> None:
    name_diagram = name_value_diagram("name", '"Anna"', caption="name не «содержит» текст, а указывает на него")

    body = f"""
{learning_outcomes(
        "объяснить присваивание как связывание имени со значением, а не помещение значения в «коробку»;",
        "предсказать результат повторного присваивания одному имени;",
        "отличить сообщение о синтаксической ошибке от traceback ошибки времени выполнения и исправить пример.",
    )}
    <h2 id="peremennye">Первые переменные: как Python на самом деле их хранит</h2>
    <p>Уже в следующей главе вы начнёте активно пользоваться переменными. Один маленький, но
    важный мысленный образ стоит освоить прямо сейчас — он убережёт от путаницы позже.</p>
{code_block("peremennaya.py", 'name = "Anna"\nprint(name)')}
    <p>Легко (и ошибочно) представлять переменную как «коробку», в которую положили значение.
    На самом деле удобнее думать иначе: <strong>имя — это ярлык, который указывает на
    значение</strong>, а не контейнер, где значение физически лежит.</p>

{name_diagram}

{callout(
        "info",
        "[[icon:experiment]] Что происходит",
        "<code class=\"inline\">name = \"Anna\"</code> означает «пусть имя <code "
        "class=\"inline\">name</code> теперь указывает на значение <code "
        "class=\"inline\">\"Anna\"</code>», а не «положи текст внутрь коробки по имени name». "
        "Подробное устройство памяти Python — тема для более поздних глав; сейчас важно только "
        "не привыкнуть к неверному образу «коробки».",
    )}

{exercise(1, "[[icon:experiment]] Эксперимент", "В практике этого раздела создайте переменную "
      "<code class=\"inline\">name</code> со своим именем и выведите её через "
      "<code class=\"inline\">print(name)</code>. Затем присвойте <code class=\"inline\">name</code> "
      "другое значение и выведите снова — обратите внимание, что «ярлык» просто стал указывать "
      "на другое значение.")}

    <h2 id="oshibki-eto-normalno">Ошибки — это нормально</h2>
    <p>Рано или поздно любая программа выдаёт ошибку — и это не признак того, что что-то пошло
    катастрофически не так. Разберём это на примере прямо сейчас, пока ставки низкие.</p>
{code_block("oshibka.py", 'print("Hello"')}
    <p>В этом коде не хватает закрывающей скобки. Python не может построить корректную программу
    и сообщает о <strong>SyntaxError</strong>, указывая место, где обнаружил проблему. Это
    синтаксическая ошибка до начала выполнения, поэтому обычной строки
    <code class="inline">Traceback (most recent call last)</code> здесь, как правило, нет.
    <strong>Traceback</strong> появляется, когда выполнение уже началось и исключение дошло до
    среды запуска без обработки; с таким примером вы встретитесь в главе 3.</p>

{callout(
        "warning",
        "[[icon:warning]] Типичная ошибка новичка",
        "Увидев сообщение об ошибке, легко решить, что «что-то сломалось навсегда». На самом "
        "деле это диагностическая информация. Для SyntaxError сначала прочитайте последнюю "
        "строку и посмотрите на указатель места ошибки; для исключения времени выполнения "
        "прочитайте последнюю строку traceback и затем найдите последнюю строку своего кода.",
    )}

    <p>Полезная привычка при виде любой ошибки — начать с <strong>последней строки</strong>: там
    обычно названы тип и причина. Затем двигайтесь вверх и ищите строку собственного кода.
    Указатель показывает место, где Python обнаружил проблему; настоящая причина иногда находится
    чуть раньше, например у незакрытой скобки или кавычки.</p>

{exercise(2, "[[icon:debug]] Debug Lab", "В практике этого раздела найдёте специально сломанную "
      "строку кода. Прочитайте сообщение SyntaxError, найдите место проблемы и исправьте её "
      "самостоятельно — так же, как в разделе «Типичная ошибка» предыдущей практики этой главы.")}
{self_check(
        "Предскажите вывод: <code class=\"inline\">name = \"Анна\"</code>, затем "
        "<code class=\"inline\">name = \"Олег\"</code>, затем <code class=\"inline\">print(name)</code>. "
        "После этого объясните, почему незакрытая скобка даёт SyntaxError до выполнения программы.",
        "Будет выведено <code class=\"inline\">Олег</code>: имя <code class=\"inline\">name</code> было "
        "повторно связано с новым строковым объектом. Незакрытая скобка нарушает грамматику, поэтому Python не "
        "может завершить разбор исходного кода и не переходит к выполнению инструкций.",
    )}"""

    out = render_page(
        page_title="Первые переменные и первые ошибки",
        description="Честный мысленный образ переменной как имени, указывающего на значение, — и "
        "первый спокойный опыт чтения сообщения об ошибке (traceback) в Python.",
        depth=2,
        breadcrumb=[("Python с нуля", "../../index.html"), ("Глава 1", "index.html"), ("Первые переменные и первые ошибки", "")],
        kicker="Глава 1 · А вы знали?",
        h1="Первые переменные и первые ошибки",
        lede="Один правильный мысленный образ переменной — и первая спокойная встреча с "
        "сообщением об ошибке, задолго до того, как оно застанет вас врасплох.",
        body_html=body,
        sidebar_groups=sidebar("01-07-peremennye-i-oshibki.html"),
        nav=PageNav(prev_href="01-02-python-eto-veselo.html", prev_label="Python — это весело!", next_href="01-03-kak-poluchit-maksimum.html", next_label="Как получить максимум от книги"),
    )
    write("01-07-peremennye-i-oshibki.html", out)


def build_03() -> None:
    body = f"""
{learning_outcomes(
        "применить цикл «предсказать → запустить → сравнить → объяснить» к новому примеру;",
        "восстановить без подсказки ключевые модели первой главы;",
        "составить конкретный план перехода к установке Python в главе 2.",
    )}
    <p>Четыре простые привычки сделают чтение книги гораздо эффективнее:</p>
    <ul>
      <li><strong>Печатайте код руками</strong>, а не копируйте. Опечатка, которую вы находите и
        исправляете сами, учит куда лучше, чем код, который просто сработал с первого раза.</li>
      <li><strong>Меняйте числа и слова в примерах</strong> — что произойдёт, если поставить
        не 100, а 300? Предсказание результата <em>до</em> запуска — лучшая тренировка.</li>
      <li><strong>Не пропускайте раздел «Типичная ошибка».</strong> Там разобраны настоящие
        ошибки начинающих — увидеть их заранее полезнее, чем встретить их вслепую.</li>
      <li><strong>Открывайте доступную практику по ссылке на странице.</strong> Теория на сайте
        объясняет идею, а интерактивный ноутбук помогает проверить её руками. В этой главе один
        общий ноутбук 01-01; в следующих главах практических ноутбуков будет больше.</li>
    </ul>

{callout(
        "tip",
        "Если что-то не работает",
        "Это не признак того, что программирование не для вас — это самая обычная часть "
        "процесса. В главе 3 и далее в книге разобрана целая система: как читать сообщение об "
        "ошибке и находить причину, а не бояться её.",
    )}

    <h2 id="itogi">Итоги</h2>
{summary_box("Что мы узнали в этой главе", [
        "Программа — это точная последовательность команд для компьютера; исходный код "
        "превращает в действия <strong>интерпретатор</strong>, а внутри CPython код по пути "
        "ещё и компилируется в байт-код.",
        "Python появился в CWI в конце 1989 года — его создал Гвидо ван Россум как развитие "
        "идей языка ABC; название — в честь «Летающего цирка Монти Пайтона».",
        "Python 3 намеренно несовместим местами с Python 2 — эта цена была осознанным выбором "
        "ради исправления давних проблем языка; поддержка Python 2 закончилась в 2020 году.",
        "python.org — официальный сайт, docs.python.org — официальная документация "
        "(Tutorial/Library Reference/Language Reference), PyPI — каталог сторонних пакетов.",
        "Python Software Foundation поддерживает проект и его инфраструктуру; техническое "
        "управление и процесс PEP фиксируют решения об изменениях; «дзен Python» "
        "(<code class=\"inline\">import this</code>) описывает ценности языка.",
        "CPython — основная реализация Python, но не единственная: есть также PyPy и "
        "MicroPython для разных задач.",
        "Python используется в самых разных областях — но не является лучшим выбором "
        "абсолютно везде, и это нормально для любого языка.",
        "Имя переменной указывает на значение, а не «содержит» его физически. SyntaxError "
        "возникает при разборе кода, а traceback показывает путь необработанного исключения "
        "времени выполнения; оба сообщения помогают исправить программу.",
        "В главе 2 вы установите Python на свой компьютер и начнёте писать код за пределами "
        "браузера.",
    ])}
{self_check(
        "Закройте предыдущие страницы и объясните пять терминов: исходный код, CPython, PyPI, PEP и связывание "
        "имени. Затем напишите один пример, где Python уместен, и один — где требуется другой инструмент.",
        "Ответ считается полным, если исходный код отделён от выполняющей реализации; PyPI — от стандартной "
        "библиотеки; PEP — от роли PSF; присваивание описано как связывание имени; выбор инструмента содержит "
        "конкретное ограничение, например предсказуемую задержку жёсткого реального времени.",
    )}
    """

    out = render_page(
        page_title="Как получить максимум от этой книги",
        description="Четыре привычки для эффективного чтения книги и полные итоги первой главы.",
        depth=2,
        breadcrumb=[("Python с нуля", "../../index.html"), ("Глава 1", "index.html"), ("Как получить максимум от книги", "")],
        kicker="Глава 1 · А вы знали?",
        h1="Как получить максимум от этой книги",
        lede="Несколько привычек, которые сделают чтение куда эффективнее, — и полные итоги "
        "главы 1.",
        body_html=body,
        sidebar_groups=sidebar("01-03-kak-poluchit-maksimum.html"),
        nav=PageNav(prev_href="01-07-peremennye-i-oshibki.html", prev_label="Первые переменные и первые ошибки", next_href="../glava-02/index.html", next_label="Глава 2: Давайте установим Python!"),
    )
    write("01-03-kak-poluchit-maksimum.html", out)


if __name__ == "__main__":
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    build_opener()
    build_01()
    build_04_history()
    build_05_ecosystem_site()
    build_06_community_philosophy()
    build_02()
    build_07_variables_errors()
    build_03()
