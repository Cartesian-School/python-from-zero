#!/usr/bin/env python3
"""Строит Главу 24: «Что дальше?» (site/chapters/glava-24/)."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from site_lib import (
    ChapterSectionLink,
    NavItem,
    PageNav,
    SidebarGroup,
    callout,
    render_chapter_opener,
    render_page,
    summary_box,
)

ROOT = Path(__file__).resolve().parent.parent
OUT_DIR = ROOT / "site" / "chapters" / "glava-24"

PAGES = [
    ("index.html", "Обзор главы"),
    ("24-01-idei-mini-proektov.html", "Идеи мини-проектов"),
    ("24-02-idei-itogovyh-proektov.html", "Идеи итоговых проектов"),
    ("24-03-chto-izuchat-dalshe-itogi.html", "Что изучать дальше. Итоги"),
]


def sidebar(active_href: str) -> list[SidebarGroup]:
    items = [NavItem(title, href) for href, title in PAGES]
    for it in items:
        it.active = it.href == active_href
    return [SidebarGroup("Глава 24 · Что дальше?", items)]


def write(name: str, html_out: str) -> None:
    path = OUT_DIR / name
    path.write_text(html_out, encoding="utf-8")
    print(f"Записано: {path.relative_to(ROOT)}")


def build_opener() -> None:
    out = render_chapter_opener(
        chapter_num=24,
        baseline_page=539,
        title="Что дальше?",
        description="Книга заканчивается — но путешествие в программирование только начинается. Куда двигаться дальше.",
        meta_items=["⏱ ~1 час", "💡 идеи проектов", "🧭 карта дальнейшего пути"],
        sections=[
            ChapterSectionLink("24.1", "Идеи мини-проектов, которые можно попробовать", "24-01-idei-mini-proektov.html", "539"),
            ChapterSectionLink("24.2", "Идеи итоговых проектов, которые можно попробовать", "24-02-idei-itogovyh-proektov.html", "540"),
            ChapterSectionLink("24.3", "Что изучать дальше", "24-03-chto-izuchat-dalshe-itogi.html", "542"),
            ChapterSectionLink("", "Итоги", "24-03-chto-izuchat-dalshe-itogi.html#itogi", "543"),
        ],
    )
    write("index.html", out)


def build_01() -> None:
    body = """
    <h2>Идеи мини-проектов, которые можно попробовать</h2>
    <p>Лучший способ закрепить всё, что вы прошли в этой книге, — придумывать и собирать
    собственные небольшие программы. Вот несколько идей, чтобы начать. Все инструменты для
    них у вас уже есть.</p>

    <h2>Приложение для конвертации валют</h2>
    <p>Похоже на конвертер температуры из главы 23, только вместо °C/°F/K — рубли, доллары
    и евро. Курсы валют можно на первых порах просто зашить в словарь прямо в коде
    (<code class="inline">KURSY = {"USD": 95.0, "EUR": 103.0}</code>), а позже, если
    захочется, — научиться получать их из интернета.</p>

    <h2>Гонка в Pygame</h2>
    <p>Машинка (прямоугольник или картинка) едет по дороге, стрелки влево/вправо двигают её
    между полосами, а сверху появляются препятствия — точно так же, как враги в
    «Космическом шутере» из главы 21, только вместо стрельбы нужно уворачиваться.</p>

    <h2>Другие узоры в Turtle</h2>
    <p>Глава 12 показала спирали и мандалы — а что, если менять не только угол поворота, но
    и цвет пера на каждом шаге? Или рисовать не круги, а звёзды разного размера по кругу?
    Попробуйте взять код мандалы и заменить одну строчку — и посмотреть, что получится.</p>
    """
    out = render_page(
        page_title="Идеи мини-проектов",
        description="Три идеи для самостоятельных мини-проектов: конвертер валют, гонка в Pygame и новые узоры в Turtle.",
        depth=2,
        breadcrumb=[("Python с нуля", "../../index.html"), ("Глава 24", "index.html"), ("Идеи мини-проектов", "")],
        kicker="Глава 24 · Что дальше?",
        h1="Идеи мини-проектов, которые можно попробовать",
        lede="Все инструменты для этих идей вы уже освоили — осталось только начать.",
        body_html=body,
        sidebar_groups=sidebar("24-01-idei-mini-proektov.html"),
        nav=PageNav(prev_href="index.html", prev_label="Обзор главы", next_href="24-02-idei-itogovyh-proektov.html", next_label="Идеи итоговых проектов"),
    )
    write("24-01-idei-mini-proektov.html", out)


def build_02() -> None:
    body = f"""
    <h2>Идеи итоговых проектов, которые можно попробовать</h2>
    <p>Если мини-проектов уже мало — вот идеи для проектов покрупнее, каждый из которых
    объединяет несколько приёмов из разных глав книги.</p>

    <h2>Игра «Змейка» в Pygame</h2>
    <p>В главе 19 змейка была на Turtle. Попробуйте собрать её заново на Pygame — движение
    списком сегментов (глава 11), столкновения через <code class="inline">colliderect()</code>
    (глава 21), еда, которая появляется в случайном месте (<code class="inline">random.randint()</code>,
    глава 5). Получится интересно сравнить два подхода к одной и той же игре.</p>

    <h2>Уклонись от пули</h2>
    <p>Обратная версия «Космического шутера» из главы 21: вместо того чтобы стрелять по
    врагам, нужно уворачиваться от падающих сверху предметов, управляя персонажем внизу
    экрана. Чем дольше продержался — тем выше счёт.</p>

    <h2>Игра на память в Pygame</h2>
    <p>Классическая игра «Найди пару»: на экране — сетка перевёрнутых карточек, клик
    переворачивает две из них, если символы совпали — карточки остаются открытыми, если нет
    — переворачиваются обратно. Пригодятся двумерные списки (глава 11) и обработка кликов
    мыши (глава 20).</p>

    {callout(
        "tip",
        "С чего начать любой из этих проектов",
        "Не пытайтесь написать всё сразу. Разбейте проект на маленькие шаги — как делали "
        "все проекты в этой книге: сначала нарисовать статичную картинку, потом добавить "
        "движение, потом — правила игры, и в последнюю очередь — счёт и «Игра окончена».",
    )}
    """
    out = render_page(
        page_title="Идеи итоговых проектов",
        description="Три идеи для более крупных итоговых проектов: Змейка на Pygame, уклонение от пуль, игра на память.",
        depth=2,
        breadcrumb=[("Python с нуля", "../../index.html"), ("Глава 24", "index.html"), ("Идеи итоговых проектов", "")],
        kicker="Глава 24 · Что дальше?",
        h1="Идеи итоговых проектов, которые можно попробовать",
        lede="Более крупные проекты, объединяющие приёмы сразу из нескольких глав книги.",
        body_html=body,
        sidebar_groups=sidebar("24-02-idei-itogovyh-proektov.html"),
        nav=PageNav(prev_href="24-01-idei-mini-proektov.html", prev_label="Идеи мини-проектов", next_href="24-03-chto-izuchat-dalshe-itogi.html", next_label="Что изучать дальше. Итоги"),
    )
    write("24-02-idei-itogovyh-proektov.html", out)


def build_03() -> None:
    body = f"""
    <h2>Что изучать дальше</h2>
    <p>Эта книга — начало пути, а не весь путь. Вот четыре направления, куда можно
    двигаться дальше, когда мини- и итоговые проекты станут получаться уверенно.</p>

    <h2>ООП подробно</h2>
    <p>Глава 15 показала основы классов и объектов. Дальше стоит изучить наследование
    (когда один класс достраивает другой), магические методы вроде
    <code class="inline">__str__</code> и <code class="inline">__eq__</code>, а также
    принципы вроде инкапсуляции — они превращают классы из удобной коробки для данных в
    по-настоящему мощный инструмент проектирования программ.</p>

    <h2>Регулярные выражения</h2>
    <p>Модуль <code class="inline">re</code> позволяет искать и проверять текст по
    шаблонам — например, «похоже ли это на email-адрес» или «вытащи все числа из строки»
    — то, что было бы очень неудобно делать вручную через методы строк из главы 8.</p>

    <h2>Веб-разработка</h2>
    <p>Глава 22 показала только самые основы Flask. Дальше — базы данных (чтобы данные не
    исчезали после перезапуска сервера), формы регистрации и входа, полноценные сайты
    с несколькими страницами и общий фреймворк вроде Django для больших проектов.</p>

    <h2>Пакеты подробно</h2>
    <p>Мы уже пользовались пакетами — <code class="inline">pygame</code>,
    <code class="inline">flask</code>. Следующий шаг — научиться собирать
    <em>свой собственный</em> пакет из написанного кода, чтобы переиспользовать его в разных
    проектах или поделиться им с другими через <a href="https://pypi.org">PyPI</a>.</p>

    {summary_box("Что мы узнали в этой книге", [
        "Python — от первой программы print(\"Привет, мир!\") до полноценных игр на "
        "Pygame и сайта на Flask.",
        "Основы языка: переменные, числа и строки, условия и циклы, списки/кортежи/"
        "множества/словари.",
        "Функции и объектно-ориентированное программирование — как упаковывать код в "
        "переиспользуемые, понятные блоки.",
        "Графика и интерфейсы: Turtle, Tkinter и Pygame — три разных инструмента для трёх "
        "разных типов программ.",
        "Файлы, обработка ошибок и веб-разработка — то, что превращает учебные примеры в "
        "программы, готовые к реальному использованию.",
        "Больше двадцати работающих проектов, каждый из которых можно взять за основу для "
        "своей собственной идеи.",
    ])}

    <h2 id="itogi">Итоги</h2>
    <p>Спасибо, что прошли этот путь от первой строчки кода до собственных игр и сайта.
    Дальше — только практика: чем больше вы пишете, тем увереннее становится каждая
    следующая программа. Удачи, и до встречи в коде!</p>
    """
    out = render_page(
        page_title="Что изучать дальше. Итоги",
        description="Четыре направления для дальнейшего изучения Python и итоги всей книги.",
        depth=2,
        breadcrumb=[("Python с нуля", "../../index.html"), ("Глава 24", "index.html"), ("Что изучать дальше", "")],
        kicker="Глава 24 · Что дальше?",
        h1="Что изучать дальше",
        lede="ООП подробно, регулярные выражения, веб-разработка и пакеты — четыре двери, открытые этой книгой.",
        body_html=body,
        sidebar_groups=sidebar("24-03-chto-izuchat-dalshe-itogi.html"),
        nav=PageNav(prev_href="24-02-idei-itogovyh-proektov.html", prev_label="Идеи итоговых проектов", next_href="../../predmetnyj-ukazatel.html", next_label="Предметный указатель"),
    )
    write("24-03-chto-izuchat-dalshe-itogi.html", out)


if __name__ == "__main__":
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    build_opener()
    build_01()
    build_02()
    build_03()
